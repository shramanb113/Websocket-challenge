# Server Configuration
PORT=8080
ENV=development

# Database Configuration
# Format: postgres://<user>:<password>@<host>:<port>/<db_name>?sslmode=disable
DATABASE_URL=postgres://postgres:password@localhost:5432/chat_app?sslmode=disable

# Auth Secrets (We'll use these tomorrow for JWT/Cookies)
AUTH_SECRET=your-super-secret-recovery-phrase-here